// import {create, connect, register, registerWithOptions, broker} from '@chimidev/micro-home'

// // First create the app instance
// const app = create(options, port, address)

// register(eventName, handler, app) // returns decorated app instance
// register(anotherEvent, handler, app) // Allows for compositions
// registerWithOptions // ??

// connect(brokerEndpoint, app)

// create({}, address, port)
// |> register(eventName, handler)
// |> register(anotherEvent, handler
// |> connect(brokerEndpoint)

// broker(options, port, address)
