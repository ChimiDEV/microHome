import { sendTcpPackage } from './network';

// Util function to send JSON RPC requests to nodes

// await sendTCP(
//   { timeout: 3000 },
//   5100,
//   address,
//   asBuffer(request('123', 'µHome/add', { param1: 10, param2: 11 })),
// )

// export const request =
